# QA Merker Tool (Visual Regression AI-QA)

Инструмент для автоматизированного визуального регрессионного тестирования сайтов при миграциях. Использует **Playwright** для создания полностраничных скриншотов и **GPT-4o Vision** для интеллектуального поиска багов.

## 🚀 Возможности

- **Автоматическое сравнение**: Загрузите список URL через CSV, и программа сравнит "Прод" и "Тест".
- **Интеллектуальный анализ (AI)**: Использование GPT-4o для поиска текстовых ошибок, неверных шрифтов и отсутствующих блоков.
- **Адаптивность**: Проверка на разных разрешениях (Desktop, Tablet, Mobile).
- **Чистые скриншоты**: Возможность скрыть куки-баннеры, чаты и другие мешающие элементы по CSS-селекторам.
- **Детальные отчеты**: Генерация стильных HTML-отчетов с "закрепленными" заголовками для удобства сравнения длинных страниц.
- **Поддержка Basic Auth**: Позволяет тестировать стенды, закрытые паролем.
- **Скачивание архивов**: ZIP-архив со всеми отчетами для удобной работы на хостинге.

---

## 🛠 Требования

- Python 3.10 или выше
- Ключ OpenAI API (опционально, для AI-анализа)

---

## 📦 Установка (локально)

### 1. Клонируйте репозиторий:
```bash
git clone <url-вашего-репозитория>
cd QA-merker-tool
```

### 2. Создайте и активируйте виртуальное окружение:
```bash
# MacOS / Linux:
python3 -m venv venv
source venv/bin/activate

# Windows:
python -m venv venv
.\venv\Scripts\activate
```

### 3. Установите зависимости:
```bash
pip install -r requirements.txt
```

### 4. Установите браузер Playwright:
```bash
playwright install chromium
```

---

## ☁️ Развёртывание на хостинге

### Streamlit Cloud (рекомендуется)

1. Загрузите проект на GitHub
2. Перейдите на [share.streamlit.io](https://share.streamlit.io)
3. Подключите ваш репозиторий
4. Добавьте файл `packages.txt` в корень проекта (уже создан):
   ```
   chromium
   chromium-driver
   ```
5. В настройках приложения добавьте секрет `OPENAI_API_KEY` (если нужен AI-анализ)

### Другие платформы (Heroku, Railway, Render)

Убедитесь, что на сервере установлены системные зависимости:
```bash
# Для Linux (Ubuntu/Debian):
apt-get install -y chromium chromium-driver

# Установка зависимостей Playwright:
playwright install-deps
```

---

## ⚙️ Настройка

### OpenAI API Key (для AI-анализа)

Укажите ключ OpenAI одним из способов:

1. **Переменная окружения** — создайте файл `.env` в корне проекта:
   ```env
   OPENAI_API_KEY=sk-ваш_ключ_здесь
   ```

2. **В интерфейсе** — введите ключ в боковой панели приложения.

3. **Секреты хостинга** — для Streamlit Cloud добавьте в Secrets:
   ```toml
   OPENAI_API_KEY = "sk-ваш_ключ_здесь"
   ```

---

## 🚀 Запуск приложения

```bash
streamlit run app.py
```

После запуска приложение откроется в браузере по адресу `http://localhost:8501`.

---

## 📖 Инструкция по использованию

### Шаг 1: Подготовьте CSV со списком URL

Создайте файл (например, `urls.csv`) со следующей структурой:

```csv
prod_url,test_url
https://mysite.com/,https://test-mysite.com/
https://mysite.com/about,https://test-mysite.com/about
https://mysite.com/contacts,https://test-mysite.com/contacts
```

> ⚠️ **Важно**: Колонки должны называться именно `prod_url` и `test_url`.

### Шаг 2: Настройте параметры в боковой панели

| Параметр | Описание |
|----------|----------|
| **Enable AI Analysis** | Включает автоматический поиск багов через GPT-4o |
| **OpenAI Model** | Выбор модели (gpt-4o, gpt-4o-mini, gpt-4-turbo) |
| **Viewport Configuration** | Разрешения экрана для тестирования (1440px, 1024px, 768px, 375px) |
| **CSS Selectors to Hide** | CSS-селекторы элементов для скрытия (баннеры, чаты, хедеры) |
| **Test Env Credentials** | Логин/пароль для Basic Auth на тестовом стенде |

### Шаг 3: Запустите сравнение

1. Загрузите CSV файл через поле **Upload CSV**
2. Нажмите кнопку **Start Comparison**
3. Дождитесь завершения (прогресс отображается в интерфейсе)

### Шаг 4: Скачайте результаты

После завершения доступны кнопки:

| Кнопка | Действие |
|--------|----------|
| **📦 Download All Reports (ZIP)** | Скачивает архив со ВСЕМИ HTML-отчетами |
| **📄 Download This Report** | Скачивает отдельный HTML-отчет для текущей пары |

### Структура ZIP-архива:
```
qa_reports_20251230_223906.zip
├── 1440px/
│   ├── root_report_1440px.html
│   ├── about_report_1440px.html
│   └── contacts_report_1440px.html
├── 768px/
│   └── ...
└── 375px/
    └── ...
```

### Шаг 5: Анализируйте AI-результаты (если включено)

- Под каждым результатом отображается вердикт AI
- **PASS** — различий не обнаружено
- **Bugs** — список найденных проблем с фрагментами скриншотов

---

## 📁 Структура проекта

```
QA-merker-tool/
├── app.py              # Основной код приложения
├── requirements.txt    # Python-зависимости
├── packages.txt        # Системные зависимости для Streamlit Cloud
├── .env               # API ключи (не коммитить!)
└── README.md          # Документация
```

---

## 🔧 Технологический стек

| Компонент | Технология |
|-----------|------------|
| **UI** | Streamlit |
| **Браузерная автоматизация** | Playwright (Chromium) |
| **AI-анализ** | OpenAI GPT-4o Vision |
| **Обработка данных** | Pandas |
| **Работа с изображениями** | Pillow (PIL) |

---

## ❓ FAQ

### Почему скриншоты отличаются?
Убедитесь, что скрыты динамические элементы (баннеры, чаты) через CSS-селекторы.

### AI не работает — что делать?
1. Проверьте корректность API-ключа
2. Убедитесь, что выбрана правильная модель
3. Проверьте баланс на аккаунте OpenAI

### Как добавить Basic Auth?
Заполните поля **Test Username** и **Test Password** в сайдбаре. Credentials применяются только к тестовым URL.

---

## 📄 Лицензия

MIT License

---

*Разработано для повышения качества и скорости QA при миграциях сайтов.*
